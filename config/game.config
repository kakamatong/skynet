-- config/game.config
return {
    -- 游戏服务器配置
    game_server = {
        node_id = 1,            -- 节点ID
        server_id = 1,          -- 服务器ID
        server_name = "测试服",  -- 服务器名称
        max_online = 2000,      -- 最大在线人数
        save_interval = 300,    -- 存档间隔(秒)
    },
    
    -- 网关配置
    gate = {
        port = 8888,           -- 监听端口
        max_client = 2000,     -- 最大连接数
        nodelay = true,        -- TCP nodelay
        client_max_buffer = 1024 * 1024, -- 客户端缓冲区大小
    },
    
    -- 登录服务配置
    login = {
        port = 8889,           -- 登录端口
        timeout = 10,          -- 登录超时时间(秒)
        max_queue = 1000,      -- 最大排队人数
    },
    
    -- 数据库配置
    database = {
        mysql = {
            host = "192.168.1.88",
            port = 23306,
            user = "user_game",
            password = "wlj2675596688",
            database = "game",
            max_packet_size = 1024 * 1024,
            pool_size = 8,
        },
        redis = {
            host = "192.168.1.88",
            port = 26379,
            auth = nil,
            db = 0,
            pool_size = 8,
        }
    },
    
    -- 聊天配置
    chat = {
        history_size = 100,    -- 历史消息数量
        cd_time = 3,          -- 发言冷却时间(秒)
        max_length = 200,     -- 最大消息长度
    }
} 